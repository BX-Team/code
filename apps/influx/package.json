{
  "name": "@bx-team/influx",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.12.15"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
